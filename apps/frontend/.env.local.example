# Local dev environment overrides (gitignored).
# Copy to .env.local when running `npm run dev` outside docker.

# Backend URL used during server-side rendering.
# In docker-compose this is set to http://backend:8000.
BACKEND_URL=http://localhost:8000
