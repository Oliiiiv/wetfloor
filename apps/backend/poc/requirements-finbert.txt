# Phase 0 dependencies for the FinBERT de-risk POC.
# Kept separate from the main requirements.txt — torch is big (~2 GB)
# and the IBKR connection POC doesn't need it.
transformers>=4.40.0
torch>=2.2.0
